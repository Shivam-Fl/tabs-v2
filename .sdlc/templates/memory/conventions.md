<!-- sdlc:stub -->
# Conventions

_Not written yet._ Seeded by `sdlc install`, which cannot know this project's conventions: a
scan sees files, not the rules a team follows. Until someone writes them, follow the code
around the change you are making, and do not invent a style it does not already have.

Write here what a newcomer's first pull request gets wrong: naming, error handling, how tests
are written and run, what a PR description must say. The Librarian adds to this as the system
learns, but it starts from what you write. Delete the marker line at the top once it is real.
